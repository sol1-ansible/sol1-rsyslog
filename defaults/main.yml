# rsyslog_packages: # Override default list of packages to install. 
rsyslog_additional_packages: []  # Additional packages to install

# Port/protocol the server listens on
rsyslog_listener_port: 514
rsyslog_listener_proto: # udp | tcp | relp | imtls
  - udp   
  - tcp   

# Forwarding options
rsyslog_forward_enable: false
rsyslog_forward_target: "127.0.0.1"
rsyslog_forward_port: 1514
rsyslog_forward_protocol: "tcp"

# Optional firewall management (firewalld)
rsyslog_configure_firewall: true

# Where remote logs are stored
# rsyslog_remote_root: /var/log/remote
# rsyslog_template_remote_log:
#   - name: "RemoteLogsNamed"
#     type: "list"
#     properties:
#       - 'constant(value="{{ rsyslog_remote_root }}/")'
#       - 'property(name="HOSTNAME")'
#       - 'constant(value="/")'
#       - 'property(name="PROGRAMNAME")'
#       - 'constant(value="-")'
#       - 'property(name="$YEAR")'
#       - 'constant(value="-")'
#       - 'property(name="$MONTH")'
#       - 'constant(value="-")'
#       - 'property(name="$DAY")'
#       - 'constant(value=".log")'
#   - name: "RemoteLogsUnknown"
#     type: "list"
#     properties:
#       - 'constant(value="{{ rsyslog_remote_root }}/")'
#       - 'property(name="HOSTNAME")'
#       - 'constant(value="/")'
#       - 'property(name="PROGRAMNAME")'
#       - 'constant(value="-")'
#       - 'property(name="$YEAR")'
#       - 'constant(value="-")'
#       - 'property(name="$MONTH")'
#       - 'constant(value="-")'
#       - 'property(name="$DAY")'
#       - 'constant(value=".log")'
# rsyslog_action_config_string: |
#   # Route to file based on whether programname is set
#   if ($programname == "") then {
#     action(type="omfile" DynaFile="RemoteLogsUnknown" createDirs="on")
#   } else {
#     action(type="omfile" DynaFile="RemoteLogsNamed" createDirs="on")
#   }
#
#   # type: string — properties is a plain string (the log path)
#   - name: "RemoteLogs"
#     type: "string"
#     properties: "{{ rsyslog_remote_root }}/%HOSTNAME%/%PROGRAMNAME%.log"
#     action:
#       - selector: "*.*"
#         type: "omfile"
#         options:
#           DynaFile: "RemoteLogs"
#           createDirs: "on"
